% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fddml.R
\name{fddml}
\alias{fddml}
\title{Fuzzy Difference-in-Differences with Machine Learning}
\usage{
fddml(
  Y,
  D,
  G,
  Ti,
  X,
  estimand = "both",
  method = "lasso",
  K = 5L,
  trim = "auto",
  trim_alpha = 0.1,
  cluster = NULL,
  se_type = "analytical",
  B = 1000L,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{Y}{Numeric vector of outcomes.}

\item{D}{Numeric vector of treatment (binary or continuous).}

\item{G}{Binary vector of group assignment (0/1).}

\item{Ti}{Binary vector of time period (0/1).}

\item{X}{Numeric matrix or data.frame of covariates.}

\item{estimand}{Character: \code{"wald"}, \code{"tc"}, or \code{"both"} (default).}

\item{method}{Character (\code{"lasso"}, \code{"rf"}, \code{"nn"}, \code{"ols"}) or a custom
function \code{f(Y_train, X_train, X_predict)}.}

\item{K}{Integer, number of cross-fitting folds (default 5).}

\item{trim}{Character: \code{"auto"} (data-driven, default), \code{"fixed"},
or \code{"none"}.}

\item{trim_alpha}{Numeric, fixed trimming level when \code{trim = "fixed"}.}

\item{cluster}{Optional vector of cluster identifiers for cluster-robust SEs.}

\item{se_type}{Character: \code{"analytical"} (default), \code{"cluster"}, or
\code{"bootstrap"}.}

\item{B}{Integer, bootstrap replications (default 1000).}

\item{seed}{Random seed for cross-fitting folds (default \code{NULL}).}

\item{verbose}{Logical, print progress messages (default \code{FALSE}).}
}
\value{
An object of class \code{"fddml"} containing:
\describe{
\item{estimates}{Data frame with columns: estimand, estimate, se,
ci_lower, ci_upper, p_value.}
\item{wald}{Full DML-Wald results (if requested).}
\item{tc}{Full DML-TC results (if requested).}
\item{nuisance}{Cross-fitted nuisance predictions.}
\item{trim_info}{Trimming diagnostics.}
\item{call}{The matched call.}
\item{N}{Sample size.}
\item{p}{Number of covariates.}
\item{settings}{List of estimation settings.}
}
}
\description{
Main estimation function. Estimates the LATE for fuzzy DID designs using
doubly robust GMM with cross-fitted ML nuisance estimation. Implements
both the DML-Wald and DML-TC estimators of Mena (2026).
}
\examples{
\donttest{
set.seed(42)
N <- 500
X <- matrix(rnorm(N * 5), ncol = 5)
G <- rbinom(N, 1, plogis(0.5 * X[,1]))
Ti <- rbinom(N, 1, 0.5)
D <- rbinom(N, 1, plogis(X[,1] + G + Ti * G))
Y <- 1 + X[,1] + 0.3 * G * Ti + D * (G * Ti) + rnorm(N)
fit <- fddml(Y, D, G, Ti, X, estimand = "wald", method = "ols", K = 2)
print(fit)
}

}
\references{
Mena, A. (2026). Double Debiased Machine Learning for Difference-in-Differences
under Imperfect Compliance. Working Paper, Brown University.

De Chaisemartin, C. and D'Haultfoeuille, X. (2018). Fuzzy
Differences-in-Differences. \emph{Review of Economic Studies}, 85(2), 999-1028.
}
